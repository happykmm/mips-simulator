MIPS模拟器
created by ZhongYu, https://github.com/lxdyran

1.功能说明
模拟器运行界面设计：	
	图形界面，包括MIPS编辑器、机器码编辑器、寄存器查看器、内存查看器；
	Mips<->机器码的实时自动汇编、反汇编；	
	单步执行指令，观察寄存器、内存的变化
	严密的查错和反馈提示
指令伪指令的汇编反汇编：
	R类型：add、sub 、slt	
	操作内存：lw、lh、lb、sw、sh、sb	
	其他I类型：addi、beq、bne、jr
	J类型：j、jal
	能够处理Label，宽松格式要求，支持注释


2.使用说明
    2.1 MIPS与机器码的转换
	在任意一侧的文本框中输入内容，将自动转换到另一侧。

    2.2 模拟执行
	点击“单步执行”，可在控制台中查看结果。
	若执行成功，则会输出 行号 和执行后的 寄存器值表 ；
	若执行失败，则会输出 行号 、 错误信息 和 寄存器值表 。

    2.3 内存说明
        共有80Byte的内存可用。他们均被初始化为随机数。

    2.4 跳转指令说明
	使用beq/bne/j/jr/jal指令跳转时，规定label/target为目标行号，且不能超过总行数。